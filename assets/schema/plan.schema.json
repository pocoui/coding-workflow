{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://ai-coding-workflow/schemas/plan.schema.json",
  "title": "Execution Plan",
  "description": "执行计划契约（事实来源），plan.md 由 render.mjs 渲染生成",
  "type": "object",
  "required": ["steps"],
  "additionalProperties": false,
  "properties": {
    "summary": {
      "$ref": "#/$defs/structuredText",
      "description": "计划概述"
    },
    "steps": {
      "type": "array",
      "minItems": 1,
      "items": { "$ref": "#/$defs/step" },
      "description": "执行步骤列表（按依赖顺序排列）"
    }
  },
  "$defs": {
    "structuredText": {
      "type": "object",
      "required": ["summary"],
      "additionalProperties": false,
      "properties": {
        "summary": {
          "type": "string",
          "minLength": 1,
          "maxLength": 200,
          "description": "一句话概述"
        },
        "details": {
          "type": "array",
          "items": { "type": "string", "maxLength": 300 },
          "minItems": 1,
          "maxItems": 10,
          "description": "详细要点（每条 ≤300 字，渲染为列表）"
        }
      }
    },
    "step": {
      "type": "object",
      "required": ["id", "name", "goal", "files", "verification"],
      "additionalProperties": false,
      "properties": {
        "id": {
          "type": "integer",
          "minimum": 1,
          "description": "步骤序号（从 1 开始）"
        },
        "name": {
          "type": "string",
          "minLength": 1,
          "maxLength": 50,
          "description": "步骤名称（≤50 字）"
        },
        "goal": {
          "oneOf": [
            { "type": "string", "minLength": 1 },
            { "$ref": "#/$defs/structuredText" }
          ],
          "description": "步骤目标：要做什么（结构化格式推荐，string 向后兼容）"
        },
        "files": {
          "type": "array",
          "items": { "type": "string" },
          "minItems": 1,
          "description": "本步骤涉及文件列表（创建或修改）"
        },
        "verification": {
          "type": "array",
          "minItems": 1,
          "items": { "type": "string", "maxLength": 200 },
          "description": "验收标准：怎么知道这一步做完了（每条 ≤200 字）"
        },
        "dependencies": {
          "type": "array",
          "items": { "type": "integer" },
          "description": "依赖的步骤 id 列表"
        },
        "risk": {
          "enum": ["normal", "guarded"],
          "default": "normal",
          "description": "风险等级：normal=普通，guarded=受保护（需人工确认）"
        }
      }
    }
  }
}